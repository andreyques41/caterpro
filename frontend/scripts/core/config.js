// frontend/scripts/core/config.js

// Define the base URL for the API.
// This can be easily changed for different environments (e.g., development, production).
export const API_BASE_URL = 'http://localhost:5000';
